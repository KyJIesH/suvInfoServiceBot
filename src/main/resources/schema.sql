-- CREATE TABLE IF NOT EXISTS people
-- (
--     pep_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--     pep_last_name VARCHAR(40) NOT NULL,
--     pep_name VARCHAR(40) NOT NULL,
--     pep_patronymic VARCHAR(40) NOT NULL,
--     pep_kurs_id BIGINT NOT NULL
-- );
--
-- CREATE TABLE IF NOT EXISTS events
-- (
--     ev_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
--     ev_owner_id BIGINT NOT NULL,
--     ev_data_event TIMESTAMP NOT NULL,
--     ev_level_event_id BIGINT NOT NULL,
--     ev_status_event_id BIGINT NOT NULL,
--     ev_description VARCHAR(10000)
-- );
--
-- CREATE TABLE IF NOT EXISTS events_people
-- (
--     ev_pep_event_id BIGINT NOT NULL REFERENCES events (ev_id),
--     ev_pep_people_id BIGINT NOT NULL REFERENCES people (pep_id),
--     PRIMARY KEY (ev_pep_event_id, ev_pep_people_id)
-- );
--
-- CREATE TABLE IF NOT EXISTS kurses
-- (
--     kur_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY REFERENCES people (pep_kurs_id),
--     kur_number_kurs BIGINT
-- );
--
-- CREATE TABLE IF NOT EXISTS level_events
-- (
--     lev_ev_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY REFERENCES events (ev_level_event_id),
--     lev_ev_level VARCHAR(30)
-- );
--
-- CREATE TABLE IF NOT EXISTS statuses
-- (
--     st_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY REFERENCES events (ev_status_event_id),
--     st_status VARCHAR(30)
-- );
--
-- CREATE TABLE IF NOT EXISTS roles
-- (
--     rol_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY REFERENCES users (us_role_id),
--     rol_role VARCHAR(30)
-- );
--
-- CREATE TABLE IF NOT EXISTS users
-- (
--     us_id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY REFERENCES events (ev_owner_id),
--     us_telegram_id BIGINT NOT NULL,
--     us_role_id BIGINT NOT NULL REFERENCES roles(rol_id),
--     us_use_date TIMESTAMP
-- );